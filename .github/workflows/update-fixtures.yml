name: 📅 Update Fixtures - Live Data

on:
  schedule:
    - cron: '0 */2 * * *'  # Every 2 hours
  workflow_dispatch:        # Manual trigger

jobs:
  update-fixtures:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
      
      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: 📦 Install dependencies
        run: |
          pip install requests python-dotenv
      
      - name: 🔍 Validate API Key Secret
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
        run: |
          if [ -z "$API_FOOTBALL_KEY" ]; then
            echo "❌ CRITICAL ERROR: API_FOOTBALL_KEY secret is NOT set in GitHub!"
            exit 1
          else
            echo "✅ API_FOOTBALL_KEY secret exists (length: ${#API_FOOTBALL_KEY})"
          fi
      
      - name: 📊 Fetch today's fixtures
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
        run: |
          python -c "
          import os
          import requests
          from datetime import datetime, timedelta
          import json
          
          API_KEY = os.environ['API_FOOTBALL_KEY']
          
          today = datetime.now().strftime('%Y-%m-%d')
          tomorrow = (datetime.now() + timedelta(days=1)).strftime('%Y-%m-%d')
          
          # Try all possible header formats
          header_formats = [
              {'x-apisports-key': API_KEY},
              {'x-rapidapi-key': API_KEY, 'x-rapidapi-host': 'v3.football.api-sports.io'},
              {'x-api-key': API_KEY}
          ]
          
          all_fixtures = []
          success = False
          
          for idx, headers in enumerate(header_formats):
              print(f'\\n🔍 Trying header format {idx + 1}: {list(headers.keys())}')
              
              url = f'https://v3.football.api-sports.io/fixtures?date={today}'
              
              try:
                  response = requests.get(url, headers=headers, timeout=10)
                  print(f'Status: {response.status_code}')
                  
                  if response.status_code == 200:
                      data = response.json()
                      if 'response' in data:
                          today_fixtures = data['response']
                          print(f'✅ SUCCESS! Found {len(today_fixtures)} fixtures for today')
                          all_fixtures.extend(today_fixtures)
                          
                          # Fetch tomorrow with same headers
                          url2 = f'https://v3.football.api-sports.io/fixtures?date={tomorrow}'
                          response2 = requests.get(url2, headers=headers, timeout=10)
                          if response2.status_code == 200:
                              tomorrow_fixtures = response2.json()['response']
                              print(f'✅ Found {len(tomorrow_fixtures)} fixtures for tomorrow')
                              all_fixtures.extend(tomorrow_fixtures)
                          
                          success = True
                          break
                  else:
                      print(f'❌ Failed: {response.text[:200]}')
              except Exception as e:
                  print(f'❌ Exception: {str(e)}')
          
          if not success:
              print('\\n❌ ALL HEADER FORMATS FAILED!')
              exit(1)
          
          # Save fixtures
          os.makedirs('shared/fixtures', exist_ok=True)
          with open('shared/fixtures/upcoming.json', 'w') as f:
              json.dump(all_fixtures, f, indent=2)
          
          # Save metadata
          metadata = {
              'updated_at': datetime.now().isoformat(),
              'total_fixtures': len(all_fixtures),
              'today_count': len([f for f in all_fixtures if f.get('fixture', {}).get('date', '').startswith(today)]),
              'tomorrow_count': len([f for f in all_fixtures if f.get('fixture', {}).get('date', '').startswith(tomorrow)])
          }
          
          with open('shared/fixtures/metadata.json', 'w') as f:
              json.dump(metadata, f, indent=2)
          
          print(f'\\n✅ Total: {len(all_fixtures)} fixtures saved')
          "
      
      - name: 📊 Fetch live matches
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
        run: |
          python -c "
          import os
          import requests
          import json
          
          API_KEY = os.environ['API_FOOTBALL_KEY']
          
          # Try all possible header formats
          header_formats = [
              {'x-apisports-key': API_KEY},
              {'x-rapidapi-key': API_KEY, 'x-rapidapi-host': 'v3.football.api-sports.io'},
              {'x-api-key': API_KEY}
          ]
          
          print('🔴 Fetching live matches...')
          
          for headers in header_formats:
              url = 'https://v3.football.api-sports.io/fixtures?live=all'
              response = requests.get(url, headers=headers, timeout=10)
              
              if response.status_code == 200:
                  live_matches = response.json()['response']
                  print(f'✅ Found {len(live_matches)} live matches')
                  
                  with open('shared/fixtures/live.json', 'w') as f:
                      json.dump(live_matches, f, indent=2)
                  break
          else:
              print('⚠️  Could not fetch live matches')
              with open('shared/fixtures/live.json', 'w') as f:
                  json.dump([], f)
          "
      
      - name: 💾 Commit and push updates
        run: |
          git config user.name "Fixtures Bot"
          git config user.email "fixtures@golden-betopia.com"
          
          git add shared/fixtures/*
          
          if git diff --staged --quiet; then
            echo "📭 No fixture changes"
          else
            git commit -m "📅 Update fixtures - $(date +%Y-%m-%d\ %H:%M) UTC"
            git push
            echo "✅ Fixtures updated"
          fi
      
      - name: 📊 Update Summary
        if: always()
        run: |
          echo "## 📅 Fixtures Update Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Time:** $(date +%Y-%m-%d\ %H:%M:%S) UTC" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f "shared/fixtures/metadata.json" ]; then
            echo "**Total Fixtures:** $(jq .total_fixtures shared/fixtures/metadata.json)" >> $GITHUB_STEP_SUMMARY
            echo "**Today:** $(jq .today_count shared/fixtures/metadata.json)" >> $GITHUB_STEP_SUMMARY
            echo "**Tomorrow:** $(jq .tomorrow_count shared/fixtures/metadata.json)" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ -f "shared/fixtures/live.json" ]; then
            echo "**Live Matches:** $(jq length shared/fixtures/live.json)" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⏰ **Next Update:** In 2 hours" >> $GITHUB_STEP_SUMMARY
